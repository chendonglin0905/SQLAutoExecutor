package com.developer.test;

import com.developer.core.sql.SQLAutoExecutor;

public class SQLAutoExecutorTest {

    public static void main(String[] args) {
        SQLAutoExecutor sqlAutoExecutor = new SQLAutoExecutor();

        sqlAutoExecutor.execute();
    }

}
